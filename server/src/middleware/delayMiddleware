const delayMiddleware = (req, res, next) => {
    const delay = 5000; // 3.5 seconds delay
    console.log(`Delaying request by ${delay / 1000} seconds...`);
    setTimeout(() => {
        next();
    }, delay);
};

module.exports = delayMiddleware;